# rails_study
railsの勉強

(自分用メモ)
```bash
cd hello
bundle init
# Gemfileの# gem 'rails'のコメントを外す
bundle install --path vendor/bundle
bundle exec rails new .
```

```bash
cd hello
bundle install --path vendor/bundle
bundle exec rails new .
```